# SEO category type slugs

This file exists so Vercel API functions can import the SEO category/type mapping without depending on `src` runtime bundling.

Keep it in sync with `src/shared/seo/categoryTypeSlugs.js` when adding new category/type slugs.
